---
wave: 1
depends_on: []
files_modified:
  - server/models/MenuItem.js
  - server/controllers/restaurantController.js
  - client/src/components/MenuManager.jsx
  - client/src/pages/RestaurantMenu.jsx
autonomous: true
---

# Phase 10: Menu Item Dietary Options - Implementation Plan

## Goal
Add an option to select Veg/Non-Veg/Egg for restaurant menu items.

## Tasks

### 1. Update MenuItem Database Model
```xml
<task>
  <read_first>
    - server/models/MenuItem.js
  </read_first>
  <action>
    Modify `menuItemSchema` in `server/models/MenuItem.js` to include a new field:
    ```javascript
    dietaryPreference: {
      type: String,
      enum: ['Veg', 'Non-Veg', 'Egg'],
      default: 'Veg'
    }
    ```
  </action>
  <acceptance_criteria>
    - server/models/MenuItem.js contains `dietaryPreference:` with enum `['Veg', 'Non-Veg', 'Egg']`
  </acceptance_criteria>
</task>
```

### 2. Update Backend Controllers
```xml
<task>
  <read_first>
    - server/controllers/restaurantController.js
  </read_first>
  <action>
    Update `addMenuItem` and `updateMenuItem` in `server/controllers/restaurantController.js` to handle `dietaryPreference`.
    - In `addMenuItem`, destructure `dietaryPreference` from `req.body` and pass it to `new MenuItem({...})`.
    - In `updateMenuItem`, destructure `dietaryPreference` from `req.body` and add it to the update object in `MenuItem.findOneAndUpdate`.
  </action>
  <acceptance_criteria>
    - server/controllers/restaurantController.js `addMenuItem` function extracts and saves `dietaryPreference`.
    - server/controllers/restaurantController.js `updateMenuItem` function extracts and updates `dietaryPreference`.
  </acceptance_criteria>
</task>
```

### 3. Update MenuManager UI (Restaurant Dashboard)
```xml
<task>
  <read_first>
    - client/src/components/MenuManager.jsx
  </read_first>
  <action>
    Update `MenuManager.jsx`:
    - Add `dietaryPreference: 'Veg'` to the initial `newForm` state.
    - Add a `<select name="dietaryPreference">` dropdown in the "Add New Dish" form with options "Veg", "Non-Veg", "Egg". Use `handleNewChange`.
    - Add a `<select name="dietaryPreference">` dropdown in the edit mode form. Use `handleEditChange`.
    - In the display mode (read-only view of the item), show a small badge or icon indicating the dietary preference (e.g., green dot/text for Veg, red for Non-Veg, yellow for Egg).
  </action>
  <acceptance_criteria>
    - client/src/components/MenuManager.jsx contains `<select name="dietaryPreference"` in both add and edit forms.
    - The initial `newForm` state includes `dietaryPreference`.
  </acceptance_criteria>
</task>
```

### 4. Update RestaurantMenu UI (Customer View)
```xml
<task>
  <read_first>
    - client/src/pages/RestaurantMenu.jsx
  </read_first>
  <action>
    Update `RestaurantMenu.jsx`:
    - Next to the menu item's name (`item.name`), display a visual indicator for `item.dietaryPreference`.
    - For example, a small colored dot or text badge (Green for Veg, Red for Non-Veg, Yellow for Egg).
  </action>
  <acceptance_criteria>
    - client/src/pages/RestaurantMenu.jsx references `item.dietaryPreference` in the JSX output.
  </acceptance_criteria>
</task>
```

## Verification
- Run the server and ensure no syntax errors.
- Verify that adding a new item via the restaurant dashboard correctly saves the dietary preference.
- Verify the dietary preference badge shows up on the customer menu.

## must_haves
- [ ] Backend accepts and saves `dietaryPreference` for menu items.
- [ ] Restaurant dashboard allows selecting `dietaryPreference`.
- [ ] Customer menu displays the `dietaryPreference`.
