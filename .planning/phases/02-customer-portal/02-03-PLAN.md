---
phase: 02-customer-portal
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified: 
  - server/models/Reservation.js
  - server/controllers/reservationController.js
  - server/routes/reservationRoutes.js
  - server/server.js
  - client/src/pages/Reservation.jsx
autonomous: true
requirements: [CUST-07]
must_haves:
  truths:
    - "Customer can request a table reservation for a specific date, time, and party size"
    - "System prevents duplicate/overlapping reservations for the same time slot at a restaurant"
  artifacts:
    - path: "server/models/Reservation.js"
      provides: "Reservation entity"
    - path: "server/routes/reservationRoutes.js"
      provides: "API for creating and managing reservations"
    - path: "client/src/pages/Reservation.jsx"
      provides: "Reservation request UI"
  key_links:
    - from: "client/src/pages/Reservation.jsx"
      to: "/api/reservations"
      via: "POST request"
    - from: "server/controllers/reservationController.js"
      to: "server/models/Reservation.js"
      via: "Conflict check query"
---

<objective>
Implement the table reservation system, allowing customers to book tables without conflict.

Purpose: Provide a seamless way for customers to secure a table at a restaurant.
Output: A conflict-aware reservation system (Backend + Frontend).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/models/User.js
@server/models/Restaurant.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Reservation Backend with Conflict Detection</name>
  <files>
    server/models/Reservation.js, 
    server/controllers/reservationController.js, 
    server/routes/reservationRoutes.js, 
    server/server.js
  </files>
  <behavior>
    - Reservation Request: Verify that the requested date/time is in the future.
    - Conflict Check: Ensure that the restaurant has not exceeded its capacity for that time slot.
      - Simple Logic: Check if the number of existing reservations for the same date/time is less than the restaurant's total tables (for MVP, use a default capacity if not defined in Restaurant model).
    - Create Reservation: Save the reservation for the authenticated user.
  </behavior>
  <action>
    1. Create `Reservation` model: userId (ref), restaurantId (ref), date, time, partySize, status (Pending, Confirmed, Cancelled).
    2. Implement `reservationController` with:
       - `createReservation`: Validates date/time and party size, checks for conflicts, and saves the reservation.
       - `getUserReservations`: Fetches all reservations for the user.
    3. Wire up `reservationRoutes` (protected by `verifyToken`) and register in `server.js` under `/api/reservations`.
  </action>
  <verify>
    <automated>
      curl -X POST http://localhost:5000/api/reservations -H "Authorization: Bearer ..." -d '{"restaurantId": "...", "date": "2026-05-01", "time": "19:00", "partySize": 2}' returns 201
    </automated>
  </verify>
  <done>Reservation backend is implemented with conflict detection logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Reservation Frontend</name>
  <files>
    client/src/pages/Reservation.jsx
  </files>
  <action>
    1. Create `Reservation` page:
       - Date picker and time selector.
       - Party size input.
       - "Request Reservation" button.
       - Success/Error messaging (e.g., "Time slot full").
    2. Integration: Connect the form to the `/api/reservations` endpoint.
    3. Ensure responsive design.
  </action>
  <verify>
    <automated>
      npm run test:ui -- --filter=reservations
    </automated>
  </verify>
  <done>Users can request table reservations through the UI and receive immediate feedback on availability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Reservation date, time, and party size are sent from client |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | /api/reservations | mitigate | Server-side validation to ensure date/time is in the future. |
| T-02-03-02 | Denial of Service | /api/reservations | mitigate | Limit the number of pending reservations a single user can have. |
</threat_model>

<verification>
- Try to reserve a table for a date in the past (should fail).
- Try to reserve a table for a slot that is already full (should fail).
- Successfully reserve a table and verify it's stored in the DB.
</verification>

<success_criteria>
- Reservations are created only if the time slot is available.
- Users can request reservations via a clean, responsive UI.
- Reservation data is correctly associated with the user and restaurant.
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-portal/02-03-SUMMARY.md`
</output>
