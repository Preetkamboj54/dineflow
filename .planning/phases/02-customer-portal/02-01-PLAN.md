---
phase: 02-customer-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - server/models/Restaurant.js
  - server/models/MenuItem.js
  - server/controllers/restaurantController.js
  - server/routes/restaurantRoutes.js
  - server/server.js
  - client/src/pages/Restaurants.jsx
  - client/src/pages/RestaurantMenu.jsx
autonomous: true
requirements: [CUST-01, CUST-02, CUST-03]
must_haves:
  truths:
    - "Customer can browse and search restaurants by name or cuisine"
    - "Customer can view a specific restaurant's menu with items and prices"
    - "Customer can search for food items across all restaurants"
  artifacts:
    - path: "server/models/Restaurant.js"
      provides: "Restaurant entity"
    - path: "server/models/MenuItem.js"
      provides: "Menu item entity"
    - path: "server/routes/restaurantRoutes.js"
      provides: "API for restaurant and menu access"
    - path: "client/src/pages/Restaurants.jsx"
      provides: "Restaurant listing and search UI"
    - path: "client/src/pages/RestaurantMenu.jsx"
      provides: "Menu display UI"
  key_links:
    - from: "client/src/pages/Restaurants.jsx"
      to: "/api/restaurants"
      via: "fetch/axios"
    - from: "client/src/pages/RestaurantMenu.jsx"
      to: "/api/restaurants/:id/menu"
      via: "fetch/axios"
---

<objective>
Implement the discovery phase of the customer journey: finding restaurants and exploring their menus.

Purpose: Allow users to discover where to eat and what to order.
Output: Fully functional restaurant browsing and menu viewing system (Backend + Frontend).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/models/User.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Restaurant and Menu Backend</name>
  <files>
    server/models/Restaurant.js, 
    server/models/MenuItem.js, 
    server/controllers/restaurantController.js, 
    server/routes/restaurantRoutes.js, 
    server/server.js
  </files>
  <action>
    1. Create `Restaurant` model: name, cuisine, address, phone, image, rating, isOpen.
    2. Create `MenuItem` model: restaurantId (ref), name, description, price, category, image, isAvailable.
    3. Implement `restaurantController` with:
       - `getRestaurants`: List all restaurants with optional search by name/cuisine.
       - `getRestaurantById`: Get details of one restaurant.
       - `getRestaurantMenu`: Get all items for a restaurant.
       - `searchFoodItems`: Global search for items across all restaurants.
    4. Wire up `restaurantRoutes` and register them in `server.js` under `/api/restaurants`.
  </action>
  <verify>
    <automated>
      curl "http://localhost:5000/api/restaurants" returns 200
      curl "http://localhost:5000/api/restaurants/search?q=pizza" returns 200
    </automated>
  </verify>
  <done>API endpoints for browsing, menu viewing, and global search are functional and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Restaurant Discovery Frontend</name>
  <files>
    client/src/pages/Restaurants.jsx, 
    client/src/pages/RestaurantMenu.jsx
  </files>
  <action>
    1. Create `Restaurants` page:
       - Search bar for name/cuisine.
       - Grid of restaurant cards showing basic info.
       - Clicking a card navigates to the menu page.
    2. Create `RestaurantMenu` page:
       - Display restaurant header info.
       - List menu items grouped by category.
       - Display price and description for each item.
    3. Ensure responsive design (mobile-first).
  </action>
  <verify>
    <automated>
      npm run test:ui -- --filter=restaurants (or manual verification if no UI tests exist)
    </automated>
  </verify>
  <done>Users can search for restaurants and view their menus via the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted search queries and IDs cross here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | /api/restaurants/:id | mitigate | Ensure only public restaurant data is returned, no internal admin flags |
| T-02-01-02 | Denial of Service | /api/restaurants/search | mitigate | Implement basic query length limits and pagination |
</threat_model>

<verification>
- Verify that searching for a cuisine returns only restaurants of that cuisine.
- Verify that viewing a menu loads the correct items for the selected restaurant.
- Verify that global food search returns items from multiple restaurants.
</verification>

<success_criteria>
- API returns correct restaurant/menu data.
- UI displays restaurants and menus responsively.
- Search functionality (restaurant and food) works as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-portal/02-01-SUMMARY.md`
</output>
