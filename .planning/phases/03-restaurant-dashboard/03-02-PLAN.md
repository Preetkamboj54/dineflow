---
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified: ["server/server.js", "server/sockets/orderSocket.js", "client/src/pages/RestaurantDashboard.jsx", "client/src/components/OrderKanban.jsx"]
autonomous: true
---

# Plan 03-02: Socket.io integration for real-time order queue

## Goal
Implement real-time order tracking using Socket.io and build a Kanban board UI to display and update order statuses. Include browser notifications for new orders.

## Requirements
- REST-02: Restaurant staff can view incoming orders in a real-time queue (Socket.io)
- REST-03: Restaurant staff can Accept or Reject incoming orders
- REST-04: Restaurant staff can update order statuses (Preparing, Ready, Completed)

## Context
- **UI:** Kanban board with New, Preparing, Ready, Completed columns.
- **Notifications:** Browser notifications on `new_order`.

## Tasks

<task>
  <description>Setup Socket.io on Server</description>
  <read_first>
    - server/server.js
    - server/sockets/orderSocket.js
  </read_first>
  <action>
    In `server/server.js`, initialize `socket.io` and attach it to the HTTP server. Create `server/sockets/orderSocket.js` to handle connections. Authenticate sockets by validating the JWT from `socket.handshake.auth.token`. Implement `socket.join(restaurantId)` so staff only get their own orders.
  </action>
  <acceptance_criteria>
    - `server/server.js` requires and initializes `socket.io`.
    - `server/sockets/orderSocket.js` implements JWT authentication.
  </acceptance_criteria>
</task>

<task>
  <description>Broadcast Order Updates</description>
  <read_first>
    - server/controllers/orderController.js
  </read_first>
  <action>
    Update `orderController.js` methods (`createOrder`, `updateOrderStatus`). After successfully saving the order to the database, emit `new_order` and `order_status_update` events to the specific restaurant's room (`io.to(restaurantId).emit(...)`).
  </action>
  <acceptance_criteria>
    - `server/controllers/orderController.js` imports socket instance or receives it.
    - Contains `io.to(...).emit(...)` calls.
  </acceptance_criteria>
</task>

<task>
  <description>Build Order Kanban UI</description>
  <read_first>
    - client/src/components/OrderKanban.jsx
  </read_first>
  <action>
    Create `OrderKanban.jsx`. Connect to Socket.io (`io()` with auth token). Listen for `new_order` and `order_status_update` to update local state. Render 4 columns: New, Preparing, Ready, Completed. Map orders into their respective columns. Add buttons to move orders between columns, triggering `PUT /api/orders/:id/status`.
  </action>
  <acceptance_criteria>
    - `client/src/components/OrderKanban.jsx` connects to `socket.io-client`.
    - Contains columns for New, Preparing, Ready, Completed.
  </acceptance_criteria>
</task>

<task>
  <description>Implement Browser Notifications</description>
  <read_first>
    - client/src/components/OrderKanban.jsx
  </read_first>
  <action>
    In `OrderKanban.jsx` (or a dedicated hook), request `Notification.permission` on mount. Inside the `new_order` socket listener, if permission is granted, trigger `new Notification('New Order!', { body: '...' })`.
  </action>
  <acceptance_criteria>
    - `client/src/components/OrderKanban.jsx` contains `new Notification(...)`.
  </acceptance_criteria>
</task>
