---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/package.json, server/server.js, server/config/db.js, server/models/User.js]
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "Server starts successfully"
    - "Database connection is established"
  artifacts:
    - path: "server/server.js"
      provides: "Express server entry point"
      min_lines: 20
    - path: "server/config/db.js"
      provides: "MongoDB connection setup"
    - path: "server/models/User.js"
      provides: "Mongoose User schema"
      contains: "mongoose.model('User'"
  key_links:
    - from: "server/server.js"
      to: "server/config/db.js"
      via: "imports and calls connectDB"
---

<objective>
Set up the backend environment, establish the MongoDB connection, and define the User data model.

Purpose: Provide the foundation for authentication and subsequent features.
Output: Initialized Express server, database connection logic, and User schema.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Initialize server directory and dependencies</name>
  <files>server/package.json</files>
  <read_first></read_first>
  <action>Create `server` directory. Initialize npm (`npm init -y`). Install dependencies: `express`, `mongoose`, `dotenv`, `cors`, `bcryptjs`, `jsonwebtoken`. Install `nodemon` as a dev dependency. Add `"start": "node server.js"` and `"dev": "nodemon server.js"` scripts to package.json.</action>
  <verify>Check that `server/package.json` contains the installed dependencies.</verify>
  <acceptance_criteria>
    - `server/package.json` contains 'express' and 'mongoose'
    - `server/package.json` contains 'dev' script
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Configure MongoDB connection</name>
  <files>server/config/db.js, server/.env.example</files>
  <read_first>server/package.json</read_first>
  <action>Create `server/.env.example` with `PORT=5000`, `MONGO_URI=mongodb://127.0.0.1:27017/dineflow`, `JWT_SECRET=your_jwt_secret`. Create `server/config/db.js` that imports mongoose and exports an async `connectDB` function which connects to `process.env.MONGO_URI` and logs success/failure.</action>
  <verify>Check that `db.js` exports `connectDB`.</verify>
  <acceptance_criteria>
    - `server/config/db.js` contains `mongoose.connect`
    - `server/.env.example` contains `MONGO_URI`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Setup Express Server Entry Point</name>
  <files>server/server.js</files>
  <read_first>server/config/db.js</read_first>
  <action>Create `server/server.js`. Import express, dotenv, cors, and `connectDB` from `config/db.js`. Call `dotenv.config()`, `connectDB()`, add `app.use(cors())` and `app.use(express.json())`. Start listening on `process.env.PORT` or 5000.</action>
  <verify>Run `node server.js` to ensure no syntax errors.</verify>
  <acceptance_criteria>
    - `server/server.js` contains `app.listen`
    - `server/server.js` contains `connectDB()`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Create User Model</name>
  <files>server/models/User.js</files>
  <read_first></read_first>
  <action>Create `server/models/User.js`. Define a Mongoose schema with `name` (String, required), `email` (String, required, unique), `phoneNumber` (String, required), `passwordHash` (String, required), and `role` (String, enum: ['customer', 'restaurant', 'admin'], default: 'customer'). Set timestamps to true. Export the User model.</action>
  <verify>Check that `User.js` exports a mongoose model.</verify>
  <acceptance_criteria>
    - `server/models/User.js` contains `mongoose.model('User'`
    - `server/models/User.js` contains `enum: ['customer', 'restaurant', 'admin']`
  </acceptance_criteria>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd server && npm run dev` starts without crashing.
- [ ] Mongoose connects to the database successfully.
</verification>

<success_criteria>
- Backend environment is configured.
- Database connection logic is ready.
- User schema accurately reflects the PRD requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>